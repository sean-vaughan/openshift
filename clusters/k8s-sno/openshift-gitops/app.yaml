
# This app.yaml file is a patch to the `openshift-gitops` argo application
# resource, which is generated by the /apps/app-of-apps/kustomize-apps.yaml argo
# application set. The data in this `app.yaml`` file are merged (strategic
# merge) into the default `spec.templates` in
# /apps/app-of-apps/kustomize-apps.yaml. At the time of this writing, this was
# the default appset template for reference:
      # metadata:
      #   name: '{{ .values.name }}'
      #   namespace: openshift-gitops
      # spec:
      #   project: "{{ .values.cluster }}"
      #   source:
      #     repoURL: 'git@github.com:sean-vaughan/openshift.git'
      #     targetRevision: HEAD
      #     path: '{{ .values.path }}' # DEFAULTS TO /apps/<app-name>, not /clusters/<cluster-name>/<app-name>
      #   destination:
      #     server: "https://kubernetes.default.svc"
      #     namespace: 'openshift-gitops' # default namespace
      #   syncPolicy:
      #     automated:
      #       prune: false
      #       selfHeal: false
      #     retry:
      #       backoff:
      #         duration: 5s
      #         factor: 2
      #         maxDuration: 3m0s
      #       limit: 2
      #     syncOptions:
      #       - CreateNamespace=true
      #       - ApplyOutOfSyncOnly=true

app:
  spec:
    source:
      # targetRevision: HEAD
      path: clusters/k8s-sno/openshift-gitops
    # syncPolicy:
    #   automated:
    #     prune: false
    #     selfHeal: false
    #   retry:
    #     limit: 3
